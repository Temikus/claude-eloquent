// This function adds two numbers together and returns the result to the caller.
// It exists because the previous implementation used a different approach that
// was harder to read, and the win here is that everything is now inline.
// Increment the counter by one each time through the loop, then return it.
function add(a, b) {
  return a + b;
}
